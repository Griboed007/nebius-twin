# (Blog draft — ≥600 words, tag #NebiusServerlessChallenge, link the repo)

Outline:
1. Problem — telematics twins are usually black boxes; you can't *ask* them.
2. Idea — a SAREF/RDF twin you query with SPARQL, fed by serverless ML.
3. Architecture — Job (batch enrich) + Endpoint (serve), shared model core.
4. Implementation — synthetic-first data, hybrid predictor, provenance invariant.
5. Running on Nebius — Jobs/Endpoints, what the preview constraints taught us.
6. Results — anomaly AUC, example SPARQL answers, runtime/cost.
7. Reproduce it — one command, offline.
