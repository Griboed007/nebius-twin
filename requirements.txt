# Minimum versions; run `pip freeze > requirements.lock` after install for the
# exact reproducibility pins judges will use.
pydantic>=2.7
numpy>=1.26
pandas>=2.2
pyarrow>=16.0
rdflib>=7.0
fastapi>=0.110
uvicorn>=0.29
httpx>=0.27
scikit-learn>=1.4
pytest>=8.0
# optional (residual model only):
# torch>=2.2
