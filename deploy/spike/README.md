# Platform spike (proposal 000, throwaway)
A trivial container run once as a Job and an Endpoint to prove auth, registry,
bucket mount, and port exposure work on Nebius BEFORE the real services exist.
Delete after capturing one job log line and one HTTP 200. Ships nothing.
